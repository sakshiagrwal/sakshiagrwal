name: Update Quote

on:
  schedule:
    # This workflow runs at 00:00 (UTC time) every day, except for Tuesdays and Fridays.
    - cron: "0 0 * * 0,1,3,4,6"
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  update_quote:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Get Random Quote
        id: quote
        run: |
          quote_data=$(curl -s "https://api.quotable.io/random")
          echo "QUOTE=$(echo "$quote_data" | jq -r '.content')" >> $GITHUB_ENV
          echo "AUTHOR=$(echo "$quote_data" | jq -r '.author')" >> $GITHUB_ENV

      - name: Update README File
        run: |
          sed -i "13s|.*|#### 📚 _${QUOTE}_|" README.md
          sed -i "15s|.*|###### ✏️ _by ${AUTHOR}_|" README.md

      - name: Commit Changes
        run: |
          git config user.email "${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com"
          git config user.name "${{ github.actor }}"
          git add README.md
          git commit -m "Update quote"

      - name: Push Changes
        run: git push origin main
